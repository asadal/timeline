<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>어드민 페이지</title>
    <!-- Tailwind CSS 설정 -->
    <script>
      tailwind.config = {
        darkMode: 'class',
        theme: {
          extend: {
            fontFamily: {
              'gmarket': ['GmarketSansMedium', 'sans-serif'],
              'noto': ['"Noto Sans KR"', 'sans-serif'],
            },
          },
        },
        plugins: [],
      }
    </script>
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts (Noto Sans KR) -->
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR&display=swap" rel="stylesheet">
    <!-- 외부 CSS 파일 연결 -->
    <link href="/css/styles.css" rel="stylesheet">
</head>
<body class="bg-gray-100 dark:bg-gray-900 transition-colors duration-300">
    <div class="container mx-auto p-4">
        <h1 class="text-4xl font-gmarket mb-6 text-center text-blue-600 dark:text-blue-400">어드민 페이지</h1>

        <!-- 웹사이트 설정 섹션 -->
        <div class="bg-white p-6 rounded-lg shadow-md mb-8">
            <h2 class="text-2xl font-semibold mb-4">웹사이트 설정</h2>
            <form action="/admin/update-settings" method="POST">
                <div class="mb-4">
                    <label for="site_title" class="block text-gray-700">웹사이트 제목</label>
                    <input type="text" id="site_title" name="site_title" value="<%= settings.site_title %>" class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring focus:ring-blue-500" required>
                </div>
                <div class="mb-4">
                    <label for="site_description" class="block text-gray-700">웹사이트 설명</label>
                    <textarea id="site_description" name="site_description" rows="4" class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring focus:ring-blue-500" required><%= settings.site_description %></textarea>
                </div>
                <button type="submit" class="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700">설정 저장하기</button>
            </form>
        </div>

        <!-- 타임라인 항목 추가 섹션 -->
        <div class="bg-white p-6 rounded-lg shadow-md mb-8">
            <h2 class="text-2xl font-gmarket mb-4">타임라인 항목 추가</h2>
            <form action="/admin/add" method="POST" enctype="multipart/form-data">
                <div class="mb-4">
                    <label for="date" class="block text-gray-700 mb-2">날짜</label>
                    <input type="date" id="date" name="date" required class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300">
                </div>
                <div class="mb-4">
                    <label for="time" class="block text-gray-700 mb-2">시간</label>
                    <input type="time" id="time" name="time" required class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300">
                </div>
                <div class="mb-4">
                    <label for="title" class="block text-gray-700 mb-2">제목</label>
                    <input type="text" id="title" name="title" required class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300">
                </div>
                <div class="mb-4">
                    <label for="body" class="block text-gray-700 mb-2">본문</label>
                    <textarea id="body" name="body" rows="4" required class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300"></textarea>
                </div>
                <div class="mb-4">
                    <label for="image" class="block text-gray-700 mb-2">이미지 업로드</label>
                    <input type="file" id="image" name="image" accept="image/*" class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300">
                </div>
                <div class="mb-4">
                    <label for="external_image" class="block text-gray-700 mb-2">외부 이미지 링크</label>
                    <input type="url" id="external_image" name="external_image" placeholder="https://example.com/image.jpg" class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300">
                </div>
                <div class="mb-4">
                    <label for="link" class="block text-gray-700 mb-2">더 알아보기 링크</label>
                    <input type="url" id="link" name="link" placeholder="https://example.com" class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300">
                </div>
                <div class="mb-4 flex items-center">
                    <input type="checkbox" id="show_time" name="show_time" class="mr-2">
                    <label for="show_time" class="text-gray-700">날짜 옆에 시간 표시</label>
                </div>
                <button type="submit" class="px-4 py-2 bg-green-600 text-white rounded-md hover:bg-green-700">타임라인 추가하기</button>
            </form>
        </div>

        <!-- CSV 업로드 섹션 -->
        <div class="bg-white p-6 rounded-lg shadow-md mb-8">
            <h2 class="text-2xl font-gmarket mb-4">CSV 파일 업로드 (타임라인 일괄 등록)</h2>
            <form action="/admin/upload-csv" method="POST" enctype="multipart/form-data">
                <input type="file" name="csvfile" accept=".csv" class="mb-4">
                <button type="submit" class="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700">업로드</button>
            </form>
        </div>
        
        <!-- 타임라인 목록 섹션 -->
<div class="bg-white dark:bg-gray-800 p-6 rounded-lg shadow-md">
    <h2 class="text-2xl font-gmarket mb-4 text-gray-800 dark:text-gray-200">타임라인 목록</h2>
    <% if (timeline.length === 0) { %>
        <p class="text-gray-600 dark:text-gray-400">등록된 타임라인 항목이 없습니다.</p>
    <% } else { %>
        <ul>
            <% timeline.forEach(function(item) { %>
                <li class="mb-6 border-b pb-4">
                    <div class="flex justify-between items-center mb-2">
                        <span class="text-gray-600 dark:text-gray-400 font-noto text-base sm:text-lg md:text-xl">
                            <% if (item.show_time) { %>
                                <%= item.date %> <%= item.time %>
                            <% } else { %>
                                <%= item.date %>
                            <% } %>
                        </span>
                        <div>
                            <a href="/admin/edit/<%= item.id %>" class="text-blue-600 dark:text-blue-400 hover:underline mr-4">수정</a>
                            <form action="/admin/delete/<%= item.id %>" method="POST" class="inline">
                                <button type="submit" onclick="return confirm('정말로 삭제하시겠습니까?');" class="text-red-600 dark:text-red-400 hover:underline">삭제</button>
                            </form>
                        </div>
                    </div>
                    <h3 class="text-xl font-semibold text-gray-900 dark:text-white font-gmarket mb-2 text-lg sm:text-2xl md:text-3xl">
                        <%= item.title %>
                    </h3>
                    <p class="text-lg font-normal text-gray-500 dark:text-gray-400 font-noto mb-2 text-base sm:text-lg md:text-xl">
                        <%= item.body %>
                    </p>

                    <% if (item.image || item.external_image) { %>
                        <div class="mb-4">
                            <% if (item.image) { %>
                                <img src="<%= item.image %>" alt="타임라인 이미지" class="w-full max-w-full mb-2">
                            <% } else if (item.external_image) { %>
                                <img src="<%= item.external_image %>" alt="외부 이미지" class="w-full max-w-full mb-2">
                            <% } %>
                        </div>
                    <% } %>

                    <% if (item.link) { %>
                        <a href="<%= item.link %>" target="_blank" class="text-green-600 dark:text-green-400 hover:underline">더 알아보기</a>
                    <% } %>
                </li>
            <% }); %>
        </ul>
    <% } %>
</div>


    </div>
</body>
</html>

