<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>타임라인 수정</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/projectnoonnu/noonfonts_2107@1.0/GmarketSansLight.woff2">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col items-center p-4">

    <div class="w-full max-w-4xl">
        <h1 class="text-4xl font-bold mb-6 text-center font-gmarket">타임라인 수정</h1>

        <div class="bg-white p-6 rounded-lg shadow-md">
            <form action="/admin/edit/<%= timeline.id %>" method="POST" enctype="multipart/form-data">
                <input type="hidden" name="currentImage" value="<%= timeline.image %>">

                <div class="mb-4">
                    <label for="datetime" class="block text-gray-700 mb-2">날짜 및 시간</label>
                    <input type="datetime-local" id="datetime" name="datetime" value="<%= new Date(timeline.date).toISOString().slice(0, 16) %>" required class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300">
                </div>

                <div class="mb-4">
                    <label for="title" class="block text-gray-700 mb-2">제목</label>
                    <input type="text" id="title" name="title" value="<%= timeline.title %>" required class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300">
                </div>

                <div class="mb-4">
                    <label for="body" class="block text-gray-700 mb-2">본문</label>
                    <textarea id="body" name="body" rows="4" required class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300"><%= timeline.body %></textarea>
                </div>

                <!-- 이미지 업로드 또는 외부 이미지 URL 추가 -->
                <div class="mb-4">
                    <label class="block text-gray-700 mb-2">이미지 업로드 또는 외부 이미지 주소</label>
                    <div class="flex items-center">
                        <input type="file" id="image" name="image" accept="image/*" class="px-3 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300 mr-4">
                        <input type="url" id="external_image" name="external_image" placeholder="외부 이미지 URL" class="px-3 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300 w-full">
                    </div>
                    <p class="text-sm text-gray-500">이미지 업로드 또는 외부 이미지 URL을 입력하세요.</p>
                </div>

                <div class="mb-4">
                    <label for="link" class="block text-gray-700 mb-2">더 알아보기 링크</label>
                    <input type="url" id="link" name="link" value="<%= timeline.link %>" placeholder="https://example.com" class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-300">
                </div>

                <div class="mb-4 flex items-center">
                    <input type="checkbox" id="show_time" name="show_time" <%= timeline.show_time ? 'checked' : '' %> class="mr-2">
                    <label for="show_time" class="text-gray-700">날짜 옆에 시간 표시</label>
                </div>

                <button type="submit" class="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700">수정하기</button>
            </form>
        </div>
    </div>

</body>
</html>

